# JavaScript 8 种基本数据类型

1. string

2. number

3. boolean

4. object

对象，一种无序键值对的集合

5. null

null 值表示一个空对象指针。

6. undefined

意为未定义

7。 bigint

8. symbol

## 存放位置：

基本数据类型：基本类型值在内存中占据固定大小，直接存储在`栈`内存中的数据
引用数据类型：引用类型`在栈中存储了指针`，这个指针指向`堆`内存中的地址，`真实的数据存放在堆内存里`。
引用数据类型中的基本数据类型：存放在`栈`中，该对象在堆内存中存储的是一个指向这个基本数据值的引用。

# typeof 操作符

1. 对一个值使用 typeof 操作符会返回其对应数据类型的字符串
2. typeof 无法区分 object 和 null 因为都返回 object
3.

# instanceof 操作符

可以用于判断一个对象的原型链上，是否有该原型。

语法：

```js
object instanceof constructor;
```

多个窗口意味着多个全局环境，不同的全局环境拥有不同的全局对象，从而拥有不同的内置类型构造函数。这可能会引发一些问题。
