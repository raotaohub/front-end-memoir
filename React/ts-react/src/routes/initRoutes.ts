import Logs from '@/view/logs/Logs/Logs'
import routesStore from '@/store/routesStore/routesStore'
// 创建 group 的目的是为创建 菜单 层级，并给菜单的 <Link to='设置路由地址'></Link>
// 1个组 代表左侧菜单的
// 如果1个组没有子菜单（暂时不考虑）


const initRoutes = () => {
    
}
export default initRoutes
